import * as action from '../../src/action';

describe('action', () => {

  describe('runAction', () => {
    it('Hello World', () => {
      expect(action.runAction()).toEqual('hello world');
    });
  });

});
