name: 'HPC Workflow'
description: 'Action that implements the development workflow for HPC repos'
author: 'Sam Lanning <sam@samlanning.com>'
runs:
  # TODO: Switch to 'node18' when GitHub starts supporting it
  using: 'node16'
  main: 'action/dist/index.js'
branding:
  icon: 'upload-cloud'
  color: 'purple'
